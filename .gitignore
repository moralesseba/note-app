# Dependencias (se regeneran con npm install)
node_modules/

# Builds generados
dist/

# Variables de entorno — ningún .env se versiona (convención estándar).
# Los valores de deploy van en el panel de Vercel (Environment Variables).
.env
.env.local
.env.production
.env.*.local

# Logs y archivos del sistema
npm-debug.log*
*.log
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Vercel (config local del CLI, no se versiona)
.vercel/
